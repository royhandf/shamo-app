import 'package:flutter/material.dart';

class WishlishPage extends StatelessWidget {
  const WishlishPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Wishlish Page'),
    );
  }
}
